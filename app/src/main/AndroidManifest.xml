<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dabkick.dabtv.happybirthdaynelson">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoBackgroundShazbot">
        <activity
            android:name=".ToTheMoonAlice"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:stateNotNeeded="true">

            <!--android:screenOrientation="locked" -->
            <!-- android:theme="@style/FullscreenTheme" -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />co
            </intent-filter>
        </activity>
        <receiver android:name="com.dabkick.dabtv.happybirthdaynelson.HdmiStuff$HdmiListener">
            <intent-filter>
                <action android:name="android.intent.action.HDMI_PLUGGED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
